# App icon (optional)

To use a custom icon for the installed app:

- **Windows**: Add `icon.ico` here (256×256 recommended). Then in `package.json` under `build.win` add `"icon": "assets/icon.ico"`.
- If no icon is set, the build uses Electron’s default icon.
